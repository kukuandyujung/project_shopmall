<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.java.mapper.MemberMapper">

<!-- 회원 정보 전체 가져오기 -->
<select id="selectAll" resultType="com.java.dto.MemberDto">
  SELECT * FROM zebra WHERE id != 0
    <if test="#{s_word} != null">
      AND (
        id LIKE '%' || #{s_word} || '%'
        OR nickname LIKE '%' || #{s_word} || '%'
        OR full_name LIKE '%' || #{s_word} || '%'
        OR gender LIKE '%' || #{s_word} || '%'
        OR phone LIKE '%' || #{s_word} || '%'
        OR email LIKE '%' || #{s_word} || '%'
        OR address LIKE '%' || #{s_word} || '%'
        OR joinday LIKE '%' || #{s_word} || '%'
        OR total_pay LIKE '%' || #{s_word} || '%'
        OR num_pay LIKE '%' || #{s_word} || '%'
      )
    </if>
  ORDER BY id ASC
</select>

<!-- 회원 정보 1개 가져오기 -->
<select id="selectOne" resultType="com.java.dto.MemberDto">
   select * from zebra where ID=#{ID}
</select>

<!--관리자 로그인 확인-->
<select id="selectLogin" resultType="com.java.dto.MemberDto" >
	select * from zebra where id=0 and password=1234
</select>

</mapper>
